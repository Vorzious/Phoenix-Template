use Mix.Config

# For development, we disable any cache and enable
# debugging and code reloading.
#
# The watchers configuration can be used to run external
# watchers to your application. For example, we use it
# with brunch.io to recompile .js and .css sources.
config :{{APP_NAME}}_endpoint, {{MODULE_NAME}}Endpoint.Endpoint,
  http: [port: 4000],
  debug_errors: true,
  code_reloader: true,
  check_origin: false,
  watchers: [
            node: ["node_modules/brunch/bin/brunch", "watch", "--stdin", #YipYip-Scaffold-Remove-CMS
                    cd: Path.expand("../../{{APP_NAME}}_cms/assets", __DIR__)], #YipYip-Scaffold-Remove-CMS
            node: ["node_modules/brunch/bin/brunch", "watch", "--stdin", #YipYip-Scaffold-Remove-WEB
                    cd: Path.expand("../../{{APP_NAME}}_web/assets", __DIR__)] #YipYip-Scaffold-Remove-WEB
            ]

# ## SSL Support
#
# In order to use HTTPS in development, a self-signed
# certificate can be generated by running the following
# command from your terminal:
#
#     openssl req -new -newkey rsa:4096 -days 365 -nodes -x509 -subj "/C=US/ST=Denial/L=Springfield/O=Dis/CN=www.example.com" -keyout priv/server.key -out priv/server.pem
#
# The `http:` config above can be replaced with:
#
#     https: [port: 4000, keyfile: "priv/server.key", certfile: "priv/server.pem"],
#
# If desired, both `http:` and `https:` keys can be
# configured to run both http and https servers on
# different ports.

# Watch static and templates for browser reloading.
config :{{APP_NAME}}_endpoint, {{MODULE_NAME}}Endpoint.Endpoint,
  live_reload: [
    patterns: [
      ~r{priv/static/.*(js|css|png|jpeg|jpg|gif|svg)$},
      ~r{priv/gettext/.*(po)$},
      ~r{lib/{{APP_NAME}}_endpoint/views/.*(ex)$},
      ~r{lib/{{APP_NAME}}_endpoint/templates/.*(eex)$},
      ~r{../{{APP_NAME}}_web/priv/static/.*(js|css|png|jpeg|jpg|gif|svg)$}, #YipYip-Scaffold-Remove-WEB
      ~r{../{{APP_NAME}}_web/priv/gettext/.*(po)$}, #YipYip-Scaffold-Remove-WEB
      ~r{../{{APP_NAME}}_web/lib/{{APP_NAME}}_web/views/.*(ex)$}, #YipYip-Scaffold-Remove-WEB
      ~r{../{{APP_NAME}}_web/lib/{{APP_NAME}}_web/templates/.*(eex)$}, #YipYip-Scaffold-Remove-WEB
      ~r{../{{APP_NAME}}_cms/priv/static/.*(js|css|png|jpeg|jpg|gif|svg)$}, #YipYip-Scaffold-Remove-CMS
      ~r{../{{APP_NAME}}_cms/priv/gettext/.*(po)$}, #YipYip-Scaffold-Remove-CMS
      ~r{../{{APP_NAME}}_cms/lib/{{APP_NAME}}_cms/views/.*(ex)$}, #YipYip-Scaffold-Remove-CMS
      ~r{../{{APP_NAME}}_cms/lib/{{APP_NAME}}_cms/templates/.*(eex)$}, #YipYip-Scaffold-Remove-CMS
    ]
  ]


