defmodule {{MODULE_NAME}}.Utils do
    import Ecto.Changeset
    # 
    # Below is an example of a Utils function to add slugs
    # 

    # def parse_slug(x) do
    #     x
    #     |> String.trim
    #     |> String.replace(~r/[\p{P}\p{S}]/, "")
    #     |> String.downcase
    #     |> String.replace(" ", "-")
    # end
    
    # def slug(x, id) when is_binary(x), do: Enum.join([id, "-", parse_slug(x)])
end
